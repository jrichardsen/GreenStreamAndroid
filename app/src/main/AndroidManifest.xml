<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.greenstream">

    <uses-permission
        android:name="android.permission.AUTHENTICATE_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission
        android:name="android.permission.GET_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission
        android:name="android.permission.MANAGE_ACCOUNTS"
        android:maxSdkVersion="22" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="de.deingreenstream.app.activities.IntroActivity" />
        <activity android:name="de.deingreenstream.app.activities.ItemListActivity" />

        <service
            android:name="de.deingreenstream.app.authentication.GreenstreamAuthenticatorService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accounts.AccountAuthenticator" />
            </intent-filter>

            <meta-data
                android:name="android.accounts.AccountAuthenticator"
                android:resource="@xml/authenticator" />
        </service>

        <activity android:name="de.deingreenstream.app.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="de.deingreenstream.app.activities.AccountActivity"
            android:windowSoftInputMode="stateHidden">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="de.deingreenstream.app.activities.MainActivity" />
        </activity>
        <activity android:name="de.deingreenstream.app.activities.AuthenticatorActivity" />
        <activity android:name="de.deingreenstream.app.activities.ViewActivity" />
        <activity
            android:name="de.deingreenstream.app.activities.SettingsActivity"
            android:label="@string/title_activity_settings" />

        <receiver
            android:name="de.deingreenstream.app.alarm.AppAlarmManager"
            android:enabled="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver android:name="de.deingreenstream.app.alarm.AlarmReceiver" />
        <receiver android:name="de.deingreenstream.app.notifications.NotificationActionReceiver" />
    </application>

</manifest>